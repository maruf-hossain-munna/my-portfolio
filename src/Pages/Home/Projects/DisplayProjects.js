import React from 'react';

const DisplayProjects = ({ project }) => {
    // const { name, bgImage } = project
    return (
        <div>
            <div className={`bg-[url{}]`}>

            </div>
        </div>
    );
};

export default DisplayProjects;